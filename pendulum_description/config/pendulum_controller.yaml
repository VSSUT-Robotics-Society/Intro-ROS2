controller_manager:
  ros__parameters:
    update_rate: 100  # Hz
    stepper:
      type: "position_controllers/JointGroupPositionController"

stepper:
  ros__parameters:
    joints:
      - shaft_joint